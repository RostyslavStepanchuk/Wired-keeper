import React from 'react';
import PropTypes from 'prop-types';

Card.propTypes = {

};

function Card(props) {
    return (
        <div>
            Card
        </div>
    );
}

export default Card;